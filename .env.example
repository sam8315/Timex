# Generate both values with:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
WEB_SECRET_KEY=<unique-random-secret-for-timex-signed-cookies>
WEB_SESSION_MIDDLEWARE_SECRET_KEY=<unique-random-secret-for-starlette-session>

# Set to 1 when Timex is served over HTTPS.
WEB_COOKIE_SECURE=0
